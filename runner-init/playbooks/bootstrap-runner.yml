---
- name: Bootstrap CloudLab runner host
  hosts: runner_hosts
  become: true
  gather_facts: true
  vars:
    runner_user: "{{ ansible_user | default(ansible_user_id) }}"
  roles:
    - runner_bootstrap
